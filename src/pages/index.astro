---
import Grid from '@components/Grid.astro';
import Layout from '@layouts/Layout.astro';

const metadata = {
  title: 'Home - NatureCare',
  description: 'NatureCare',
};
---

<Layout metadata={metadata}>
	<Grid />
</Layout>
